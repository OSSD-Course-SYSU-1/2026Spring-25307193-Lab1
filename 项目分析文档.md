# 你画我猜游戏项目分析文档

## 项目概述

这是一个基于HarmonyOS的分布式多人协作游戏"你画我猜"。项目实现了鸿蒙的"自由流转"（跨端迁移）和"一次开发，多端部署"特性，支持多设备协同游戏体验。

## 一、核心技术特性实现情况

### 1. 自由流转（Cross-Device Migration）实现情况 ✅ **已完整实现**

#### 实现机制：
- **Ability配置**：在`module.json5`中设置`"continuable": true`，使Ability支持跨端迁移
- **ContinuationManager**：专门的跨端迁移管理器，处理游戏状态的保存与恢复
- **数据迁移策略**：
  - **小数据（<100KB）**：通过`wantParam`直接传递（sessionId、currentWord等）
  - **大数据（笔迹数据）**：通过分布式数据对象的`save()`/`restore()`机制传输
- **迁移流程**：
  - **源端**：`onContinue()`中保存游戏状态到wantParam和分布式数据对象
  - **目标端**：`onCreate()`/`onNewWant()`中检测`LaunchReason.CONTINUATION`并恢复状态
- **状态恢复**：完整恢复游戏会话、笔迹、得分、答案、游戏状态和轮次

#### 关键代码文件：
- `ContinuationManager.ets` - 跨端迁移核心逻辑
- `EntryAbility.ets` - Ability生命周期中的迁移处理
- `DistributedDataObjectUtil.ets` - 分布式数据同步支持迁移

### 2. 一次开发，多端部署（One-Time Development, Multi-Device Deployment）实现情况 ✅ **已完整实现**

#### 响应式布局系统：
- **断点系统**：`sm` (<600vp), `md` (600-840vp), `lg` (≥840vp)
- **布局适配**：
  - **手机（竖屏）**：画板上方，工具栏和判定区下方
  - **平板/折叠屏（横屏）**：左侧画板，右侧工具栏+判定区+得分榜
- **组件自适应**：`DrawingToolbar`组件根据断点自动切换横向/竖向布局

#### 多设备支持：
- **module.json5配置**：支持`phone`, `tablet`, `2in1`设备类型
- **产品目录结构**：
  - `products/phone/GamePage.ets` - 移动端入口
  - `products/pad/GamePage.ets` - 平板端入口
- **窗口尺寸监听**：`Index.ets`中监听`windowSizeChange`动态调整断点

#### 自适应UI组件：
- `DrawingToolbar.ets` - 根据断点切换布局方向
- `Index.ets` - 通过`PhoneLayout()`和`TabletLayout()`Builder实现不同布局

### 3. 分布式协同能力 ✅ **已完整实现**

#### 设备发现与连接：
- **DeviceDiscovery**：使用`distributedDeviceManager`发现同一华为账号下的可信设备
- **RoomManager**：管理游戏房间创建、加入和设备连接
- **连接方式**：通过`startAbilityByCall`拉起猜题端应用并传递sessionId

#### 数据同步机制：
- **分布式数据对象**：实时同步笔迹、得分、答案、游戏状态
- **实时绘画同步**：画板端的每一笔都实时同步到所有猜题端
- **状态管理**：游戏状态（drawing/guessing/roundEnd）跨设备同步

## 二、游戏玩法与功能

### 1. 游戏角色
- **画板端（Host）**：负责出题、绘画、判断答案、计分
- **猜题端（Guesser）**：观看绘画过程、提交答案猜词

### 2. 连接方法
1. **自动设备发现**：通过HarmonyOS分布式软总线自动发现同一账号下的设备
2. **手动加入**：猜题端输入画板端的sessionId加入游戏
3. **跨设备拉起**：画板端可通过`startAbilityByCall`直接邀请猜题端

### 3. 游戏流程
1. **角色选择**：用户选择成为画板端或猜题端
2. **房间创建**：画板端创建游戏房间，生成唯一sessionId
3. **设备加入**：猜题端发现并加入房间
4. **游戏进行**：
   - 画板端从词库随机获取题目并绘画
   - 猜题端实时观看绘画并提交答案
   - 画板端判断答案对错并计分
   - 支持清屏、撤销、下一题功能
5. **得分统计**：实时显示所有玩家得分排名

### 4. 核心功能
- **实时绘画同步**：Canvas触摸事件采集+分布式数据对象同步
- **答案提交与判定**：猜题端提交答案，画板端判断对错
- **得分系统**：答对得10分，实时排名
- **词库系统**：内置15个中文词汇（熊猫、飞机、手机等）
- **振动反馈**：猜题端答对时提供振动反馈

## 三、项目架构分析

### 1. 文件结构
```
entry/src/main/ets/
├── common/
│   ├── models/          # 数据模型
│   │   ├── GameSessionData.ets    # 游戏会话数据
│   │   ├── PlayerInfo.ets         # 玩家信息
│   │   └── StrokePoint.ets        # 笔迹点
│   └── utils/
│       ├── DistributedDataObjectUtil.ets  # 分布式数据工具
│       └── VibratorUtil.ets               # 振动工具
├── entryability/
│   └── EntryAbility.ets           # 应用主Ability
├── features/
│   ├── collaboration/
│   │   ├── ContinuationManager.ets # 跨端迁移管理
│   │   ├── DeviceDiscovery.ets     # 设备发现
│   │   └── RoomManager.ets         # 房间管理
│   ├── drawing/
│   │   ├── DrawingCanvas.ets       # 画板端绘画组件
│   │   └── DrawingToolbar.ets      # 画板工具条
│   └── guessing/
│       ├── AnswerSubmit.ets        # 答案提交组件
│       ├── GuessingCanvas.ets      # 猜题端观看组件
│       └── ScoreBoard.ets          # 得分榜组件
├── pages/
│   └── Index.ets                   # 主页面
└── products/
    ├── phone/GamePage.ets          # 手机端入口
    └── pad/GamePage.ets            # 平板端入口
```

### 2. 技术栈
- **ArkUI**：声明式UI框架
- **分布式数据对象**：`@kit.ArkData`实现跨设备数据同步
- **分布式设备管理**：`@kit.DistributedServiceKit`实现设备发现
- **Ability框架**：`@kit.AbilityKit`支持跨端迁移
- **Canvas绘图**：实现绘画功能

### 3. 数据流设计
```
画板端触摸事件 → StrokePoint → 分布式数据对象 → 猜题端Canvas重绘
猜题端答案提交 → AnswerRecord → 分布式数据对象 → 画板端判定
得分变更 → 分布式数据对象 → 所有端ScoreBoard更新
```

## 四、未实现/待优化功能

### 1. 已完整实现的功能 ✅
- [x] 自由流转（跨端迁移）
- [x] 一次开发多端部署（响应式布局）
- [x] 分布式设备发现与连接
- [x] 实时绘画同步
- [x] 答案提交与判定
- [x] 得分系统与排名
- [x] 词库系统
- [x] 振动反馈
- [x] 清屏、撤销功能
- [x] 多设备类型适配（手机、平板、2in1）

### 2. 可扩展功能（未实现）⚠️
- **语音聊天**：玩家间实时语音交流
- **更多画笔工具**：目前仅支持3种颜色和粗细调整
- **游戏房间管理**：创建私有房间、密码保护
- **更多游戏模式**：计时模式、团队对战等
- **离线模式**：单机练习模式
- **数据持久化**：游戏历史记录保存
- **社交功能**：好友系统、排行榜

## 五、项目亮点

### 1. 架构设计优秀
- **清晰的职责分离**：模型、工具、组件、页面分层明确
- **松耦合设计**：组件通过Props/Events通信，易于测试和维护
- **错误处理完善**：分布式功能降级到本地模式，保证基础功能可用

### 2. 用户体验考虑周全
- **响应式设计**：完美适配不同尺寸设备
- **实时反馈**：绘画同步延迟低，答案判定即时
- **容错机制**：模拟器环境下自动降级到本地模式

### 3. 鸿蒙特性深度应用
- **自由流转**：完整实现跨设备游戏状态迁移
- **分布式能力**：充分利用HarmonyOS分布式软总线
- **一次开发多端部署**：真正实现代码复用

## 六、总结

该项目成功实现了课程要求的"自由流转"和"一次开发，多端部署"核心特性：

1. **自由流转**：通过ContinuationManager和分布式数据对象，实现了画板端游戏状态的无缝迁移，包括笔迹、得分、游戏状态等完整数据。

2. **一次开发，多端部署**：通过响应式断点系统（sm/md/lg）和自适应布局，一套代码完美适配手机、平板、折叠屏等多种设备。

3. **分布式游戏体验**：基于HarmonyOS分布式能力，实现了真正的多设备协同游戏，设备自动发现、实时数据同步、跨设备拉起等功能完善。

项目代码结构清晰，注释详细，遵循HarmonyOS最佳实践，是一个优秀的分布式应用示范项目。